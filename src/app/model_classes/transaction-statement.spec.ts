import { TransactionStatement } from './transaction-statement';

describe('TransactionStatement', () => {
  it('should create an instance', () => {
   // expect(new TransactionStatement()).toBeTruthy();
  });
});
